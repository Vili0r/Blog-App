import '../styles/globals.scss';
import 'react-quill/dist/quill.snow.css';

function App ({ Component, pageProps }) {
    return (
        <Component {...pageProps} />
    );
} 
        
export default App
